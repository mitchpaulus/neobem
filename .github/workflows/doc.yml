name: Build docs

on:
  push:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install pandoc texlive-lualatex texlive-latex-base

    - name: Build docs
      run: redo doc/all
